{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "verify": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".cache/**", "dist/**", "build/**"]
    },
    "dev": {
      "cache": false
    }
  }
}
