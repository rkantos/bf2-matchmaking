{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".cache/**"]
    },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": [".cache/**"]
    }
  }
}
